function [center, xbar]=zentriert(data)



[N,p]=size(data);   % finds number of n=rows and m=columns
l=ones(N,1); % vector of ones for mean operation

xbar=mean(data);

% means using formula in text


center=data-l*xbar;       % deviations from the mean
                  % formula [2.16]
