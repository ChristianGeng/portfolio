function y=scaleit(x,a,b);
% SCALEIT Apply Gain and offset to signal
% function y=scaleit(x,a,b);
% scaleit: Version ???
%
%   mainly to ensure signal files are scaled consistently
%   possibly expand to handle matrix input??

y=(x*a)+b;
