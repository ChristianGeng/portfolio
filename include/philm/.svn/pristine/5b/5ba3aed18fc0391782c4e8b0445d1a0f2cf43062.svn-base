function prompter_sstimd(S);
% function prompter_sstimd(S)
% Set stimulus variables (may eventually allow optional argument for specific
% field)
%most likely to be useful for handling the wavlist field
%perhaps eventually for modifying prompts during the experiment 

hf=findobj('type','figure','name','Subject');
hax=findobj(hf,'type','axes','tag','text');

set(hax,'userdata',S);
