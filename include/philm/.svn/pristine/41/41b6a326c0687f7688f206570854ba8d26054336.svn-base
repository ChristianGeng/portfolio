%rest position in trial 1 is quite a bit different from most of the
%recording
matfile='ampsadj\merge\rawpos\0125';
[dataout,descriptor,unit,dimension,slist]=loadpos_sph2cartm(matfile);
slist=slist(1:10,:);

vdistance=50;

makerefobjn(matfile,slist,vdistance);
