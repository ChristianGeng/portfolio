function prompter_swaved(A)
% function prompter_swaved(A)
% Set wave data for start and end signals (may eventually allow optional argument for specific
% field). Most likely use for setting whether output is to synchronous or
% asynchronous

hf=findobj('type','figure','name','Investigator');
haxi=findobj(hf,'type','axes','tag','wave');

set(haxi,'userdata',A);
